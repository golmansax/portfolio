$navbar-lambda = 3 * $lambda

.navbar {
  background $my-green
  height unit($navbar-lambda, em)
  position relative

  h1 {
    color $white
  }

  +e(menu) {
    position absolute
    top unit($navbar-lambda, em)
    right 0
    background $white
    border-left $colored-border

    a {
      display block
      padding unit($lambda / 2, em) unit($lambda, em)
      border-bottom $colored-border
      text-decoration none

      &.active {
        text-decoration underline
      }
    }
  }

  +e(sidebar-trigger) {
    background inherit
    border 0
    color $white
    float right
    height unit($navbar-lambda, em)
    margin-right -.9em // fudge factor to get image to line up with container
    padding 0 unit($navbar-lambda / 4, em)

    +above('l') {
      display none
    }
  }

  +e(sidebar-trigger-icon) {
    font-size unit($navbar-lambda / 2, em)
  }
}

.navbar-left {
  float left

  a {
    clearfix()
  }

  img,
  h1 {
    float left
  }

  img {
    $img-lambda = 2 * $lambda
    $img-width = unit($img-lambda, em)

    height $img-width
    margin unit(($navbar-lambda - $img-lambda) / 2, em)
    margin-left -.7em // fudge factor to get image to line up with container
    width $img-width
  }
}

.navbar-right {
  float right
  line-height $navbar-lambda

  a {
    color $white
    margin 0 unit($lambda, em)
    text-transform uppercase
    text-decoration none

    &:hover {
      text-decoration underline
    }

    &.active {
      text-decoration underline
    }

    &:last-child {
      margin-right 0
    }
  }

  +below('m') {
    display none
  }
}
