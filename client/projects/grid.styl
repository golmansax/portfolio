.projects-grid {
  +e(item) {
    padding unit($lambda, em) 0

    h2 {
      text-align center
    }

    +above('xl') {
      col(1/3, cycle: 3)
    }

    +between('m', 'l') {
      col(1/2, cycle: 2)
    }
  }
}
